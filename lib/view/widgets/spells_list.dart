import 'package:flutter/material.dart';

class SpellsList extends StatelessWidget {
  const SpellsList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
